import {GET_FOLDERS,DELETE_FOLDER,ADD_FOLDER,FOLDERS_LOADING} from '../action/types';
const initialState = {
    folders: [],
    folders_loading: false
}
export default function folderReducer(state = initialState, action)
{
    switch (action.type) {
        case GET_FOLDERS:
            
    }
}